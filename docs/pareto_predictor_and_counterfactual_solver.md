# Pareto Predictor and Counterfactual Solver

The predictor estimates local vector:
`[tracking, impact, payload vibration, oscillation, effort, limit risk]`.

The solver minimizes expert preference weighted objective with smoothing priors to produce pseudo-target `z*`.
